# M110 Popup 提示条增强实现计划

1. 扩展 popup 提示状态 API，让 `setHint` 支持 `info / success / error`
2. 先补 popup 布局/辅助函数测试，锁定 `data-tone` 与提示条样式存在
3. 调整 `popup.html` 中 footer 样式，加入状态色和小圆点
4. 更新 `popup.js`，把关键交互点改成带状态的提示调用
5. 运行 extension 测试、typecheck、build
